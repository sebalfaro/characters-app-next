{
  "name": "characters-app",
  "version": "1.0.0",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "dev": "docker-compose up --no-deps frontend-dev",
    "start": "docker-compose up -d frontend",
    "stop": "docker-compose down"
  },
  "devDependencies": {},
  "dependencies": {}
}
